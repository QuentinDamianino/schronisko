{% extends 'base.html.twig' %}

{% block body %}

    <a href="{{ path('homepage') }}">Storna Główna</a>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}

{% endblock %}